; Raised limits so the social video feed can accept short clips.
; Copied into the PHP conf.d directory by the Dockerfile.
upload_max_filesize = 30M
post_max_size = 32M
max_execution_time = 120
memory_limit = 256M
